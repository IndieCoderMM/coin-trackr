<div class="item-card">
    <div class="item-meta">
        <div class="item-icon">💸</div>
        <div class="item-group">
            <h2 class="item-name"><%= expense.name %></h2>
            <p class="item-data">
                <%= expense.updated_at.to_datetime.strftime("%B %d, %Y at %I:%M %p") %>
            </p>
        </div>
    </div>
    <div class="item-group">
        <p class="item-value">
            <%= number_to_currency(expense.amount) %>
        </p>
        <%= button_to content_tag(:i, "", class: "fa-solid fa-square-minus"), category_expense_path(category, expense), method: :delete, class: 'del-btn' %>
    </div>
</div>